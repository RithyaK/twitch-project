import styled from "styled-components";

const Following = () => {
  return <ContainerMain>Following</ContainerMain>;
};

export default Following;

const ContainerMain = styled.div`
  overflow: auto;
  width: 100%;
  color: var(--color4);
  background-color: #141416;
  padding: 20px;
`;
